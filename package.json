{
  "name": "workrigly.com",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Source for Workrigly, an English static site about free, open-source, and self-hosted software alternatives.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "test": "node scripts/verify-build.mjs",
    "deploy": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/deploy.ps1"
  },
  "devDependencies": {
    "wrangler": "^4.37.1"
  }
}
